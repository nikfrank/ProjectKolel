'use strict';

var _ = require('./');

var _2 = _interopRequireDefault(_);

var _chai = require('chai');

var _chai2 = _interopRequireDefault(_chai);

var _sinon = require('sinon');

var _sinon2 = _interopRequireDefault(_sinon);

var _sinonChai = require('sinon-chai');

var _sinonChai2 = _interopRequireDefault(_sinonChai);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

_chai2.default.use(_sinonChai2.default);

describe('null handler', function () {
  it('should call next, because it does nothing!', function () {
    var next = _sinon2.default.spy();
    var done = _sinon2.default.spy();

    // make a null handler for testing?

    var noHandler = new _2.default(next, done);

    // spy on listWala.fetcher.handleRequest

    var stub = {};

    var action = {
      network: { handler: 'NoHandler' },
      stub: stub
    };

    noHandler.handleRequest(action);

    (0, _chai.expect)(next).to.have.been.calledWith(stub);
    (0, _chai.expect)(done).to.have.been.calledOnce;
  });
});